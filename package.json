{
  "name": "aws-uncle",
  "description": "Useful uncle notifying unexpected servers in AWS",
  "version": "0.0.0",
  "author": "moqada <moqada@gmail.com>",
  "dependencies": {
    "aws-sdk": "^2.1.48",
    "babel-runtime": "^5.8.20",
    "dotenv": "^1.2.0",
    "hipchatter": "^0.2.0",
    "minimatch": "^2.0.10",
    "node-jungle": "^0.3.0"
  },
  "devDependencies": {
    "babel-eslint": "^4.1.0",
    "del": "^2.0.1",
    "eslint": "^1.3.1",
    "eslint-config-moqada": "^3.0.0",
    "fs-extra": "^0.24.0",
    "mkdirp": "^0.5.1",
    "zip-local": "^0.1.3"
  },
  "license": "MIT",
  "main": "dist/uncle.js",
  "private": true,
  "scripts": {
    "build": "babel src --out-dir dist",
    "publish": "rm -rf {build,dist} && npm run build && cp -r dist build && babel-node script/deploy.js",
    "test": "eslint src script",
    "watch": "babel src --out-dir dist --watch"
  }
}
